(()=>{const $=id=>document.getElementById(id),cfg=window.SJW_CONFIG;function uid(prefix='id_'){try{if(window.crypto&&typeof window.crypto.randomUUID==='function')return prefix+window.crypto.randomUUID().split('-').join('')}catch{}const bytes=new Uint8Array(16);try{window.crypto.getRandomValues(bytes)}catch{for(let i=0;i<bytes.length;i++)bytes[i]=Math.floor(Math.random()*256)}return prefix+Array.from(bytes,b=>b.toString(16).padStart(2,'0')).join('')}let id='';try{id=localStorage.getItem('sjw_client_id')||''}catch{}if(!id){id=uid('client_');try{localStorage.setItem('sjw_client_id',id)}catch{}}window.SJW_CLIENT_ID=id;let siteId=localStorage.getItem('sjw_site_id')||'',previewUrl='',busy=false,files=[],trialRemaining=2;const create=$('createView'),workspace=$('workspace'),details=$('detailsDialog'),publish=$('publishDialog'),limit=$('limitDialog'),overlay=$('generatingOverlay'),topbar=document.querySelector('.top');
function makeSiteId(){return uid('sjw_').slice(0,20)}function show(view){[create,workspace].forEach(v=>v.hidden=v!==view);const inWorkspace=view===workspace;document.body.classList.toggle('preview-mode',inWorkspace);if(topbar)topbar.hidden=inWorkspace}function text(id,v){const el=$(id);if(el)el.textContent=v}function validUrl(value,kind='preview'){const u=new URL(value);if(u.protocol!=='https:')throw Error('Invalid URL');if(kind==='preview'&&!((u.hostname==='api.shangjiawangzhan.com')||u.hostname.endsWith('.shangjiawangzhan.com')))throw Error('Invalid preview URL');if(kind==='stripe'&&!['checkout.stripe.com','billing.stripe.com'].includes(u.hostname))throw Error('Invalid payment URL');return u.href}async function refreshTrial(){try{const t=await SJW_API.trial();trialRemaining=t.remaining;text('trialText',`${t.remaining} free creation${t.remaining===1?'':'s'} remaining`)}catch{}}
$('images').addEventListener('change',e=>{files=[...e.target.files].filter(f=>['image/jpeg','image/png','image/webp'].includes(f.type)&&f.size<=8*1024*1024).slice(0,6);text('imageCount',files.length?`${files.length} selected`:'')});document.querySelectorAll('[data-prompt]').forEach(b=>b.onclick=()=>{$('prompt').value=b.dataset.prompt;$('prompt').focus()});$('detailsBtn').onclick=()=>details.showModal();$('langBtn').onclick=()=>{document.documentElement.lang=document.documentElement.lang==='zh-CN'?'en':'zh-CN';$('langBtn').textContent=document.documentElement.lang==='zh-CN'?'English':'中文'};
async function uploadAll(){const ids=[];for(const file of files){const r=await SJW_API.upload(file);ids.push(r.assetId)}return ids}
function startProgress(){let p=8,seconds=0,i=0;const lines=['Creating your website…','Adding your business content…','Matching images and layout…','Preparing the final preview…'];overlay.hidden=false;text('loadingText',lines[0]);text('elapsedText','0s');$('progress').style.width='8%';const timer=setInterval(()=>{seconds++;text('elapsedText',`${seconds}s`);p=Math.min(90,p+(seconds<5?8:seconds<12?4:1.5));$('progress').style.width=`${p}%`;if(i<lines.length-1&&seconds>=[3,7,13][i]){i++;text('loadingText',lines[i])}},1000);return success=>{clearInterval(timer);$('progress').style.width=success?'100%':`${p}%`;if(success)setTimeout(()=>{overlay.hidden=true},240)}}
$('createForm').addEventListener('submit',async e=>{e.preventDefault();if(busy)return;const prompt=$('prompt').value.trim(),name=$('businessName').value.trim();if(!prompt&&!name){text('message','Describe your business or add a business name.');return}if(trialRemaining<=0){limit.showModal();return}busy=true;$('generateBtn').disabled=true;text('message','');show(workspace);text('siteName',name||'Creating website');text('siteStatus','Working…');$('preview').removeAttribute('src');const done=startProgress();try{if(!siteId)siteId=makeSiteId();const imageIds=await uploadAll();const payload={siteId,prompt,business:{name,industry:$('industry').value.trim(),city:$('city').value.trim(),contact:$('contact').value.trim(),services:$('services').value.trim(),description:$('description').value.trim(),language:document.documentElement.lang==='zh-CN'?'zh':'en'},assets:{heroImageId:imageIds[0]||'',imageIds}};const r=await SJW_API.generate(payload);previewUrl=validUrl(r.previewUrl);localStorage.setItem('sjw_site_id',siteId);$('preview').onload=()=>{done(true);text('siteStatus','Preview ready');text('siteName',name||'Website');refreshTrial()};$('preview').src=previewUrl;}catch(err){done(false);overlay.hidden=true;show(create);if(err.code==='TRIAL_LIMIT'){limit.showModal()}else{text('message',err.name==='AbortError'?'Website creation timed out. Please try again.':(err.message||'Unable to create website.'))}}finally{busy=false;$('generateBtn').disabled=false}});
$('backBtn').onclick=()=>{show(create);$('prompt').focus()};$('editBtn').onclick=()=>details.showModal();$('addPhotosBtn').onclick=()=>$('workspaceImages').click();$('workspaceImages').addEventListener('change',e=>{files=[...e.target.files].filter(f=>['image/jpeg','image/png','image/webp'].includes(f.type)&&f.size<=8*1024*1024).slice(0,6);if(files.length){show(create);text('imageCount',`${files.length} selected`);$('prompt').focus()}});$('newDesignBtn').onclick=()=>{if(busy)return;show(create);$('prompt').focus();text('message','Adjust your description or generate again for a different design.')};$('shareBtn').onclick=async()=>{if(!previewUrl)return;try{if(navigator.share)await navigator.share({title:$('siteName').textContent,url:previewUrl});else{await navigator.clipboard.writeText(previewUrl);text('siteStatus','Link copied')}}catch{}};$('publishBtn').onclick=()=>publish.showModal();$('continuePreviewBtn').onclick=()=>{publish.close();if(previewUrl)window.open(previewUrl,'_blank','noopener')};
async function pay(plan,msgEl){if(busy)return;busy=true;const buttons=['monthlyBtn','yearlyBtn','limitMonthly','limitYearly'].map($).filter(Boolean);buttons.forEach(b=>b.disabled=true);text(msgEl,'Opening secure checkout…');try{if(!siteId)siteId=makeSiteId();const r=await SJW_API.checkout({plan,email:'',siteId,siteName:$('businessName').value.trim()||'Business Website',returnUrl:cfg.siteUrl});location.href=validUrl(r.url,'stripe')}catch(e){text(msgEl,e.message||'Unable to open checkout.');busy=false;buttons.forEach(b=>b.disabled=false)}}$('monthlyBtn').onclick=()=>pay('monthly','publishMessage');$('yearlyBtn').onclick=()=>pay('yearly','publishMessage');$('limitMonthly').onclick=()=>pay('monthly','limitMessage');$('limitYearly').onclick=()=>pay('yearly','limitMessage');$('manageBtn').onclick=async()=>{const email=prompt('Membership email');if(!email)return;try{const r=await SJW_API.portal({email,returnUrl:cfg.siteUrl});location.href=validUrl(r.url,'stripe')}catch(e){alert(e.message||'Unable to open membership management')}};refreshTrial();window.addEventListener('error',e=>{try{show(create);text('message','The app could not start. Please refresh and try again.')}catch{}});window.addEventListener('unhandledrejection',e=>{try{if(!busy)text('message','Something went wrong. Please try again.')}catch{}});
})();
